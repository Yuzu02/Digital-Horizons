export interface CommentProps {
  content: string;
  id: string;
  author: string;
  authorImage: string;
  createdAt: string;
  email: string;
}

export interface CommentSectionProps {
  slug: string;
}
